# Contributing

PRs that keep skills aligned with [docs.cartesia.ai](https://docs.cartesia.ai) and OpenAPI are welcome.

Prefer **links** over copying values that drift (version date lists, exact field names). **cartesia-api** is REST/WebSocket + SDKs; **line-voice-agent** is Cartesia Line (CLI, deploy, Line SDK)—do not blur that split.
